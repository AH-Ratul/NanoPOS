model Product {
    id             String     @id @default(uuid())
    name           String
    sku            String     @unique
    price          Float
    stock_quantity Int
    createdAt      DateTime   @default(now())
    updatedAt      DateTime   @updatedAt
    saleItems      SaleItem[]
}
